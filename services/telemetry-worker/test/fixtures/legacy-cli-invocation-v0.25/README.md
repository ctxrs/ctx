# Released `cli_invocation@1` fixtures

These payloads are pinned from the serializer and command-property producers in
public tag `v0.25.0`, especially `crates/ctx-cli/src/analytics.rs`, `main.rs`,
`integrations/`, and `upgrade/command.rs`. They intentionally preserve the
released legacy vocabulary rather than the current typed telemetry vocabulary.
