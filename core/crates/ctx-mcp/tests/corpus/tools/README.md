# MCP Tool Fuzz Regression Corpus

Store minimized reproducer payloads for deterministic MCP fuzz regression checks.

Update via:

```bash
bash core/scripts/update_fuzz_corpus.sh mcp <source_dir>
```
