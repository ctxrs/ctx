# Apps

`core/apps/` contains user-facing clients and shells.

- `web`: the workbench UI (web app; used by desktop and planned mobile shells)
- `desktop`: desktop wrapper/build tooling
- `tauri-mobile`: post-MVP mobile client (Tauri 2 shell wrapping the built `web` UI; mobile-specific differences like QR scanning are planned but not implemented yet)
